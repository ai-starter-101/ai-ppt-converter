# 自动化教学课程录制系统

将PPT文件转换为教学视频的自动化工具。

## 项目状态

⚠️ **当前状态**: 项目初始化完成，模块代码占位符已创建
📅 **当前阶段**: 第1步 - 项目目录结构创建
✅ **完成时间**: 2026-01-16

## 功能特性

- **PPT解析**: 提取文本、图片、表格等内容
- **脚本生成**: 基于规则的智能脚本生成
- **语音合成**: 中文女声TTS，支持多重备用方案
- **视频合成**: 1080p高清视频输出
- **批量处理**: 支持批量转换多个PPT文件

## 技术栈

- **PPT解析**: python-pptx
- **脚本生成**: 规则模板 + 可选Ollama
- **语音合成**: gTTS + espeak备用
- **视频合成**: FFmpeg
- **开发语言**: Python 3.9+

## 目录结构

```
ai-ppt-converter/
├── src/                    # 源代码
│   ├── modules/           # 核心模块
│   │   ├── ppt_parser.py        # PPT解析
│   │   ├── script_generator.py  # 脚本生成
│   │   ├── tts_engine.py        # 文本转语音
│   │   └── video_composer.py    # 视频合成
│   └── utils/            # 工具模块
│       ├── logger.py             # 日志系统
│       └── exceptions.py         # 异常处理
├── tests/                # 测试
│   ├── unit/            # 单元测试
│   ├── integration/     # 集成测试
│   └── test_ppts/      # 测试PPT文件
├── config/              # 配置
├── templates/           # 脚本模板
├── output/             # 输出文件
├── logs/               # 日志文件
├── main.py             # 主程序
├── batch_processor.py # 批量处理
├── requirements.txt    # 依赖列表
└── requirements-minimal.txt  # 最小依赖
```

## 安装

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 安装系统依赖

```bash
# macOS
brew install ffmpeg libreoffice

# Ubuntu/Debian
sudo apt-get install ffmpeg libreoffice
```

### 3. 验证安装

```bash
python main.py --help
```

## 使用方法

### 单个PPT转换

```bash
python main.py input.pptx -o output.mp4
```

### 批量处理

```bash
python batch_processor.py --input-dir ./ppt_files --output-dir ./videos
```

## 开发进度

### ✅ 已完成
- [x] 项目目录结构创建
- [x] 核心模块代码框架
- [x] 依赖配置文件
- [x] 测试框架
- [x] 日志和异常处理

### 🔄 进行中
- [ ] 第2步: 增强PPT解析器功能

### 📋 待完成
- [ ] 第3步: PPT解析器测试和优化
- [ ] 第4步: 实现PPT到图片转换
- [ ] 第5步: 设计脚本模板系统
- [ ] ... (详见实施计划)

## 测试

### 运行单元测试

```bash
pytest tests/unit/ -v
```

### 运行集成测试

```bash
pytest tests/integration/ -v
```

### 运行所有测试

```bash
pytest tests/ -v
```

## 配置

编辑 `config/default.yaml` 文件来自定义配置：

```yaml
# TTS设置
tts:
  primary_engine: "gtts"
  language: "zh-cn"
  speed: 0.9

# 视频设置
video:
  resolution: "1920x1080"
  frame_rate: 30
```

## 注意事项

- 模块代码目前为占位符状态，等待实现
- 测试PPT文件目录已创建，请将测试文件放入 `tests/test_ppts/`
- 详细实施计划请参考 `memory-bank/IMPLEMENTATION_PLAN.md`

## 许可证

MIT License

## 作者

Claude Code
